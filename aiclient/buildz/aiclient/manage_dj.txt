
考虑ai调用的方案：
发送存储策略，以及当前数据，让ai自己判断是否要处理，要处理就调用工具
可以调用工具：
获取消息列表里的数据，清理数据（处理记忆后，或者认为是无效数据）

获取记忆列表

根据主体、客体、关系、内容查询
可以模糊匹配

可以设定遍历层数

内容生成向量（向另一个向量模型调用）

增加、修改、删除记忆

获取关系代词列表
可以模糊匹配，根据key或内容查询

发送最近记忆和最近消息数据，让ai按自己的策略查询记忆拼接成对话

ai可以模糊向量匹配，然后匹配一层遍历，选择有用的再递归遍历

ai可以根据对话自主选择主体或内容，进行模糊匹配


